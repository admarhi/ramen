# Cluster a `ConsortiaMetabolismSet` Object

Modifies a `ConsortiumMetabolismSet` by clustering the binary matrices
and storing the result to the object.

## Usage

``` r
cluster(object)
```

## Arguments

- object:

  A `ConsortiumMetabolismSet` object to modify

## Value

A modified `ConsortiumMetabolismSet` object
