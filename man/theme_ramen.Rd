% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-ramen.R
\name{theme_ramen}
\alias{theme_ramen}
\title{Ramen plot theme}
\usage{
theme_ramen(baseSize = 11, baseFamily = "", network = FALSE)
}
\arguments{
\item{baseSize}{Numeric. Base font size in points. Defaults to 11.}

\item{baseFamily}{Character. Base font family. Defaults to \code{""}
(device default).}

\item{network}{Logical. If \code{TRUE}, build on top of
\code{theme_void()} instead of \code{theme_minimal()}; used by
\code{\link{plotDirectedFlow}} and \code{plot} methods that render
networks.}
}
\value{
A \code{ggplot2} theme object.
}
\description{
Shared \pkg{ggplot2} theme used across every plot returned by the
package. Provides a consistent typography, white background, bottom
legend, and tight title spacing. Network plots pass
\code{network = TRUE} to drop axes, ticks, and grid lines while
keeping the rest of the look-and-feel.
}
\examples{
library(ggplot2)
ggplot(mtcars, aes(mpg, hp)) +
    geom_point() +
    theme_ramen()
}
